{
  "name": "devops-activity-log",
  "version": "1.0.2",
  "description": "A GitHub Action to log repository activity.",
  "main": "src/index.js",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "axios": "^1.11.0",
    "yaml": "^2.8.1"
  },
  "scripts": {
  "build": "ncc build src/index.js --license licenses.txt",
  "test:unit": "echo '✔ Running mock unit tests...' && exit 0",
  "test:integration": "echo '✔ Running mock integration tests...' && exit 0"
},
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
